<h2>Contact List</h2>

<ul>
  <% @contacts.each do |contact| %>
    <li>
      <%= link_to contact.full_name, contact_path(id: contact.id) %>
      <%= link_to 'Edit', edit_contact_path(id: contact.id) %>

    </li>
  <% end %>
</ul>
